name: OSS Validate

on:
  push:
    branches:
      - main
      - "codex/**"
  pull_request:

jobs:
  validate-oss-surface:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run OSS validation matrix
        run: npm run validate:oss
