[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "alphabook"
version = "0.1.0"
description = "Hybrid corpus research loops for books and papers"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.116.0",
    "openai>=1.68.2",
    "uvicorn>=0.35.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.3.5",
]

[project.scripts]
alphabook = "alphabook.cli:main"
alphabook-agent-server = "alphabook.agent_server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/alphabook"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
